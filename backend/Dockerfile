FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi 'uvicorn[standard]' pydantic pydantic-settings python-multipart
COPY app ./app
ENV AQUARIUM_DATA_DIR=/data
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
